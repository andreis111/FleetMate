<%- include('partials/headerAdmin', {user: user}) -%>

<main id="main" class="main">

  <div class="pagetitle">
    <h1>To repair:</h1>
    <nav>
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/admin">Home</a></li>
        <li class="breadcrumb-item active">To repair</li>
      </ol>
    </nav>
</div>
    
    <ul>
        <% for (const repair of repairs) { %>
          <li class="<%= repair.completed ? 'completed' : '' %>">
            <input type="checkbox" value="<%= repair._id %>" <%= repair.completed ? 'checked' : '' %>>
            <%= repair.content %> - <%= repair.truckPlate %>
          </li>
        <% } %>
      </ul>

</main>

<a href="/logout">Logout</a>

<%- include('partials/footer') -%>